using UnityEngine;

public class Unit : MonoBehaviour
{
    // Placeholder: We will add MoveAction, SpinAction, etc. here later.
    // For now, this just needs to exist so GridObject doesn't crash.
    
    private void Start()
    {
        // Future: Register self to GridSystem
    }
}